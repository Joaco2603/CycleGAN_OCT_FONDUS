paths:
  logs: logs
  weights: weights
  generated: generated

data:
  image_size: 256
  augment: true
  num_workers: 2
  fundus:
    root: dataset/fundus
    train: train
    val: val
    test: test
  oct:
    root: dataset/oct
    train: train
    val: val
    test: test

model:
  blocks: 9

loss:
  lambda_cycle: 10.0
  lambda_identity: 0.5

optim:
  lr: 0.0002
  betas: [0.5, 0.999]
  batch_size: 4

training:
  epochs: 12
  decay_start: 100
  amp: true
  sample_interval: 100
  save_interval: 10
  max_gpu_temp: 70.0
  temp_check_interval: 2
